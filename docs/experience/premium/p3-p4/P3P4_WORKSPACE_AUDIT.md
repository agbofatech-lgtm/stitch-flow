# P4 Workspace Audit

Grammar: CONTEXT → PRIMARY TASK → WORK AREA → SUPPORTING INFORMATION → ACTIONS → STATUS.

Implemented primitives: AtelierShell, AtelierNavigation, WorkspaceHeader, WorkspaceToolbar, WorkspaceCanvas, WorkspacePanel, InspectorPanel, ContextBar, StatusBar, CommandPalette, EmptyState (ExperienceEmptyState), LoadingState, WorkspaceSkeleton, ErrorState (+ retry), ConfirmationDialog, ToastRegion.

Atelier Home: attention-first, AppContext counts only.

Design Studio: framed, not rewritten.

Measurements / Production / Operations: hosted in the shell; interiors not rebuilt (P5/P6).
