# Component Inventory

Reuse: Button, PageHeader (now level 1/2), Workroom, ErrorState+action, LoadingState, WorkspaceSkeleton, ExperienceEmptyState, Badge, FeatureGate.

Duplicate still present: per-room SummaryCard, ModalShell, FilterChip. Consolidation deferred where rewrite risk > benefit.

FeatureGate: fake Upgrade CTA removed.
