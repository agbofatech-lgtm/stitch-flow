# PEX Atelier Audit

Atelier Home answers “what needs attention” from AppContext (customers, in-progress orders, due alerts).

Does not call shop dashboard HTTP (those routes are unmounted by default).

Client Studio, Production, Business rooms still use legacy screens inside the token shell.
