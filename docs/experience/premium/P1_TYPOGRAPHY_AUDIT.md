# P1 Typography Audit

Display: Space Grotesk. Body: Inter (html base). Numeric: IBM Plex Mono + `font-numeric` / `tnum`.

Identity vs content vs data: Atelier Home uses display for greeting, numeric for order numbers.
