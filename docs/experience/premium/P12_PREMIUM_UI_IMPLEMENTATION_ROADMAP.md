# P12 — Premium UI Implementation Roadmap

**LOCKED until Owner authorizes a visual programme.** Not P19. Not Phase 20 by default.

Suggested slices (paper):

1. Hygiene: theme-color, scalable viewport, retire unused Layout from product path, token adoption on replaceable screens.
2. Shell polish: breadcrumbs, inspector always-available context WHO/WHAT/STAGE.
3. Room restyle: Dashboard/Customers/Orders/Production/Materials/Invoices/Reports onto experience primitives.
4. Hosted Studio: **external chrome only** (page padding, not engine/canvas math).
5. Imagery/type Owner decision (Satoshi vs Inter).
6. Control Center UI as **new plane**, not Settings rewrite.
7. Motion: apply existing presets; do not copy splash into every route.
8. Perf: lazy DesignStudio; splash not fake-wait if already ready — Owner call.

Each slice must re-hash protected assets.
