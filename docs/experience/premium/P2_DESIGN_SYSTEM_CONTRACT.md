# P2 Design System Contract

```
TOKEN → PRIMITIVE → COMPOSITE → WORKSPACE → PRODUCT
```

Screens must use `bg-surface-*`, `text-ink-*`, `bg-action-*`, `border-line`, `rounded-sf*`.

Exceptions: DesignStudio internals, print/PDF CSS, SVG craft illustration on splash.
