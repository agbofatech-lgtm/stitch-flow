# P1 Token Audit

Roles in `tokens.css`: canvas, workspace, panel, elevated, floating, text primary/secondary/muted/inverse, action primary/hover/secondary, status, border subtle/default/strong, focus, disabled, space 1–16, radius, elevation, duration.

Dark `[data-theme=dark]` still unused by App. **FACT.**
