# P1 Color Audit

Brand teal `#0F6E8C` remains **action primary** (identity), not scattered hex.

Replaceable screens no longer use raw `#0F6E8C` except Settings placeholder string.

Indigo EmptyState removed in P0. DesignStudio still uses hex internally (protected).
