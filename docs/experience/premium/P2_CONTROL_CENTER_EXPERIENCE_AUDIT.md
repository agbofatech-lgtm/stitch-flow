# P2 — Control Center Experience Audit

## FACT

- Backend: `/control/status`, `/tenants`, `/configuration`, `/audit`, `/billing/provider` (operators only).
- Frontend `rg /control` in `apps/web/src`: **no matches**.
- No AGBOFA Control Center application shell.

API/control plane ≠ finished Control Center.

## Required information hierarchy (NOT implemented)

EXECUTIVE OVERVIEW · TENANTS · IDENTITY · PLANS · ENTITLEMENTS · BILLING · PAYMENTS · AUDIT · CONFIGURATION · FEATURE CONTROLS · SYSTEM HEALTH · SECURITY · OPERATIONS

Settings.tsx is **tenant workspace settings**, not platform Control Center (P19 constitution).

## Visual direction (audit only)

Control Center should be denser, operational, AGBOFA-branded, distinct navigation — sharing tokens/type/status language with Atelier, not sharing Command Center CRUD.

**Control Center experience score: low because the UI does not exist.**
