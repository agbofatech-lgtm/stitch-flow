# STITCHFLOW SER-F0 — FINAL FORENSIC REPORT

## 1. Baseline

Branch `arena/01a05677-stitch-flow` HEAD `0d49f8b40d935eb90812a624ccd70d9925abaa42`. SAC-5 Level 1 inherited. Unrelated dirt preserved.

## 2. Code change declaration

APPLICATION CODE: NONE · BACKEND: NONE · DATABASE: NONE · CSS: NONE · PROTECTED ASSETS: NONE  
Documentation only under `docs/experience/ser/f0/`.

## 3. Runtime truth

Product starts at Vite `main.tsx` → `AppProvider` → Splash → `StudioShell` → `AtelierShell`. No URL router. Layout/Dashboard **not mounted**. HTML boot 200 titled **StitchFlow Digital Atelier** on :5173 and :5174. **No screenshot lab.**

## 4. Experience truth (the answer)

StitchFlow **looks like a tokenized atelier frame around several older applications.**

The owner can fail to perceive PEX because:

1. The complete primitive museum is `experience-preview.html` (unreachable from product nav).
2. Chrome (nav, type, splash, home) did change; **rooms they work in often did not**.
3. Clients / Production / Invoices likely show **ErrorState** (unmounted HTTP), so the “redesign” is a pretty header over a failure.
4. Design Studio’s protected interior still dominates Design.
5. Manifest still says **TailorPro**.
6. Motion is a 220ms fade, not a cinematic craft journey.

CODE EXISTENCE ≠ RUNTIME VISIBILITY ≠ EXPERIENCE QUALITY.

## 5. PEX forensic verdict

PEX is **structurally real in the shell (A)** and **experientially incomplete in the rooms (C/D)**. Conditional PEX certification remains fair. No numeric restyle score is assigned (no visual lab).

## 6. Cinematic truth

Splash is the only memorable beat. Room changes are BASIC. Object journey is NONE. Trusted finalize exists as a Dialog. Sync is a status badge. Classification overall: **BASIC–FUNCTIONAL**, not EXPERIENTIAL.

## 7. Thematic truth

Not one world. Atelier chrome + technical measurement table + legacy canvas + HTTP admin + KPI dashboard + dark control plane.

## 8. Data truth (do not migrate in SER)

AppContext SoT vs HTTP CRUD vs T2 vs `/shop`. Remounting unauthenticated routes is **forbidden** as an experience fix.

## 9. Protected assets

Git working tree clean for patternEngine, productionAssistant, DesignStudio, shared types, productionStageService. **PASS (unchanged).**

## 10. Recommended next (not started)

SER-F1 constitution → F2 product visual language (not preview island) → F3 meaningful motion → F4 refine existing StudioShell → F5 workrooms with SAC-safe data → Studio **frame** only → F7–F9 with a real visual lab.

## 11. SER-F1–F9

**LOCKED.**
