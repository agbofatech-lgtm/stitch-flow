# Reproducibility Model

**FACT:** T10 proves 20-run equality for pattern and for production excluding `generatedAt`.

**PROPOSAL:** same, after binding three frozen versions. Live customer mutation must not change a second run that still cites A+B+C.

Engine-internal defaults when measurements are missing remain a **conditional** reproducibility property: identical missing keys ⇒ identical engine defaults. Phase 16 must not invent substitute numbers (98/100/102) at the orchestration layer.
