# P16 Provenance Model

`ExecutionProvenance` records version ids, configuration fingerprint, T10 computation versions, T0 engine SHA identities, input fingerprint, execution result fingerprint.

Snapshot `createdAt` and snapshot `id` are metadata, not identity.
