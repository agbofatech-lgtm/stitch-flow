# P19.4 Plan Model

Plan = packaging. Not payment, entitlement, identity, tenant, role, or permission.

Legacy seed codes **BASIC / PRO / STUDIO** (`classification: legacy-seed`). Not renamed. Display names are metadata.

Future codes (FREE, ENTERPRISE, …) **not** created as winners.

`GET /platform/plans` lists code + displayName only.
