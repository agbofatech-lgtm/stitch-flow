# P19.9 Runtime Integration

`createApp` loads file store when `PLATFORM_DATA_PATH` is set. Mutating identity/commercial/control operations `persist()`.

Injected test platforms skip the env file path.
