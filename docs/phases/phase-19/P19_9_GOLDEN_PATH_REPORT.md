# P19.9 Golden Path

`tests/persistence.p19.test.ts` covers register → checkout → verified event → entitle → stale event ignored → cancel/deny → restore/allow.

Trusted core strings absent from access JSON.
