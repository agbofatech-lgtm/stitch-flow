# P19.9 Entitlement Enforcement

Server `POST /platform/access/check` remains law. Kill-switch FEATURE_DISABLED still works after restart if file-backed.

Client FeatureGate / localStorage / query params are not consulted.
