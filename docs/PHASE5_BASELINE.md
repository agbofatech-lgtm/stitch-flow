# PHASE 5 BASELINE — Commercialization audit (read-only; no application changes made)

1. **Starting commit:** `0b4c6f8af0fb1d62612808f3484ad60f8fe51b2b` (= tag `phase-4-production-hardening-complete`; verified locally, on origin, and against the remote branch head).
2. **Starting tag:** `phase-4-production-hardening-complete` (remote-verified).
3. **Branch:** `arena/01a04183-stitch-flow` — session-fixed (documented §4 constraint; branch creation unavailable; immutable tags are the recovery mechanism). `main` verified untouched at `b576c3e` on origin.
4. **Repository structure:** monorepo — `apps/web` (React/Vite PWA + Dexie offline foundation + Workbox SW), `apps/backend` (Express/PostgreSQL, migrations 001–010, embedded-PG test rig), `apps/mobile` (Capacitor scaffold, not release-audited), `apps/api` (orphan reference tree, superseded), `docs/` (Phase 1–4 documentation).
5. **Current commercial functionality:** none server-enforced. Client has a rich *presentation-only* tier system: `config/tiers.ts` (TierCode `BASIC|PRO|STUDIO`, GHS pricing metadata: PRO 45/mo, STUDIO tier), `modules/services/tierEnforcement.ts` (~12 `checkCan*` gates + limits + FEATURE_COMPARISON), `FeatureGate.tsx`, Settings plan UI — all driven by mock/local workspace state (`Workspace.billingStatus: trial|active|past_due|cancelled`, `trialExpiresAt`, `overridePlan`). **UX-only; trivially forgeable client-side; no backend checks.**
6. **Existing subscription functionality:** NO subscriptions table, NO subscription code server-side. The nearest server substrate is the Phase-2 **per-user license** system: `licenses` (tier `free|pro|enterprise`, max_devices, status active/revoked), `license_devices`, `/licenses/validate` (device fingerprint gating), admin tier updates — user-scoped, not workspace-scoped, and **its tier vocabulary conflicts with the client's** (`free/pro/enterprise` vs `BASIC/PRO/STUDIO`). This is the key Phase 5 domain-model decision (see §20 below).
7. **Existing billing functionality:** none. No provider SDKs, no webhook endpoints, no billing tables, no checkout flows. (A grep "paystack" hit was a false positive: `stripEmbeddedProfileCollections`.) No provider credentials exist in the environment.
8. **Existing entitlement functionality:** server: none for workspaces (only per-user device-count via licenses). Client: `getFeatureAccess()`/`checkRolePermission()` — UX gating only.
9. **Environment configuration:** `getEnv()` validation; secrets required (no prod fallbacks); `.env.example` placeholders only (no billing vars yet); CORS env-driven (prod allowlist); no VITE_ secrets; no `.env` committed.
10. **Deployment assumptions:** RENDER_EXTERNAL_URL hint + CI deploy-hook stub in workflows; Phase 4 deployment checklist exists; nothing actually provisioned from this environment.
11. **Migration system:** ordered SQL 001–010, `schema_migrations` tracking, per-migration transactions, `--verify`, fail-fast `verifySchema()` at boot; no runtime DDL; no advisory lock (documented P3-MIG-001).
12. **Backup strategy:** documented only (PHASE4_BACKUP_RECOVERY.md); no infrastructure reachable from this sandbox; restore never executed against real production infra (embedded-PG test cycle exercises fresh-restore-equivalent path every test run).
13. **Observability:** pino + pino-http request logs, requestId correlation in sanitized error envelope, audit_logs (auth, sync, license, payment events), admin-only audit read. No metrics/alerting stack.
14. **Rate limiting:** auth (5/15min prod), license, sync, events + general `apiRateLimit` (2000/5min); relaxed under test; no per-account differentiation (IP-based via express-rate-limit defaults).
15. **PWA:** Workbox generateSW — shell precache, navigate fallback with API denylist, zero runtime caching, autoUpdate; fail-safe registration; **no in-app update UX** (autoUpdate reload semantics only); browser-level validation NOT executable in sandbox (P2-R2 carried forward).
16. **Phase 4 P2 items:** P2-R1 (UI read path via AppContext; IndexedDB is durable mirror + sync substrate) — investigated, still open; P2-R2 (browser PWA validation) — still not executable here. Others: cursor-regression-after-restore detection, migration advisory lock, mobile config mismatch (root vs apps/mobile capacitor configs), pagination on business lists.
17. **Technical debt:** 16 protected-file lint findings (documented exceptions); apps/api orphan tree; dual tier vocabularies (client vs server); app_settings/workspace UI still partially mock-workspace based (`default-workspace` convention); business list endpoints unbounded (shop-scale).
18. **Risks for Phase 5:** (a) any subscription model must be **workspace-scoped** and must reconcile the two tier vocabularies without breaking the protected client gating UX; (b) no provider credentials exist → billing must be provider-neutral with test doubles, marked REQUIRES EXTERNAL CREDENTIALS; (c) server-side entitlement enforcement on business routes must not break the offline-first client (which tolerates 4xx by design, but limit-denials need machine-readable codes); (d) trial authority must move server-side (client `trialExpiresAt` is mock data).
19. **Test baseline (executed now, exact):** root production build PASS · web type-check 0 errors · web lint 16 findings (11 DesignStudio.tsx + 5 jobSheetExport.ts — unchanged documented protected exceptions, no new debt) · client offline tests **41/41 PASS** (5 files) · backend lint(tsc) 0 errors · backend tests **83/83 PASS** (10 suites) · Phase-1 smoke PASS. Matches the Phase 4 final report exactly — no drift.
20. **Recommended Phase 5 execution order:** (1) commercial domain migration 011: workspace-scoped `subscriptions` + `billing_events` (provider-neutral, idempotent event ids) + `usage_counters` only where limits are real; adopt ONE canonical plan vocabulary server-side (recommend aligning to client-facing `BASIC/PRO/STUDIO` with a licenses-compat mapping; decision documented before implementation); (2) server plan catalogue + `resolveEntitlements(workspaceId)` + enforcement middleware on genuinely limited operations (customer/member limits, premium export/report endpoints) with `SUBSCRIPTION_REQUIRED`/`LIMIT_EXCEEDED` codes + concurrency-safe limit checks; (3) trial lifecycle server-authoritative (subscription rows seeded `trialing` at workspace creation); (4) billing provider boundary (interface + webhook endpoint with signature verification + event idempotency; Paystack-recommended for Ghana, credentials REQUIRED EXTERNALLY; test doubles for automated tests only); (5) client: consume public entitlement representation (replace mock tier resolution as UX source; server remains authority); offline sync-status UX refinement; PWA update UX; (6) P2-R1 incremental read-path work only if it fits without destabilizing (§32 "do not perform a giant rewrite"); (7) ops docs (deployment/env/runbooks/release checklist), security matrix extension, E2E commercial workflow test, final certification.

**Discrepancy check (§7 stop rule):** repository matches the verified Phase 4 architecture exactly (tests, builds, protected files, tags). No material differences. Safe to proceed after baseline approval.
