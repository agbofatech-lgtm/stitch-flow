# T3 Domain Boundary Report

**FACT:** T3 isolates domain contracts around existing intelligence.

**FACT:** Protected engines and Design Studio were not modified. SHA-256 matches T0 registry.

**FACT:** Body vs garment vs pattern are distinct at `apps/web/src/domain/measurement`. Pattern is derived.

**FACT:** Conflict merge for measurement / order / production is implemented (`domain-merge`). Complementary fields merge; disagreements stay local and mark conflict.

**FACT:** New T3 persistence uses T2 `EntityRepository`. No new localStorage key.

**FACT:** Design Studio and AppContext still call engines directly. That is T7 extraction, not a T3 Studio rewrite.

**INFERENCE:** Wrapper equality tests imply no output drift for sampled kinds (bodice, shirt, trouser, skirt, kaftan) and a shirt production plan.

**Limitation:** AppContext localStorage remains TRANSITIONAL. Studio drafts still use existing `stitchflow:design-studio:drafts` (legacy, not added in T3).
