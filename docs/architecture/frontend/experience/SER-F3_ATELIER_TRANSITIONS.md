# SER-F3 atelier transitions

Journey order: Floor → Clients → Measurements → Design → Production → Ledger.

`workspacePreset(from, to)` uses **+x** when moving forward, **−x** when returning. Not a generic fade.

Representative runtime: Floor **Continue to measurements** → `goAtelierRoom('measurements')` (no URL router). Thread remains in the header (contextual).
