# SER-F2 atelier primitives

New: `AtelierPage`, `AtelierThread`, `AtelierConfidence`, `AtelierStage`.

Reused: Button, Dialog, Workroom, PageHeader, Panel, Empty/Error/Loading.

Not created: a primitive per HTML element.

Mounted in **product** Floor, Measurement table, StudioShell header/status — not in `experience-preview.html`.
