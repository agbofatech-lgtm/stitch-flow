# Design Studio boundary

```
PROTECTED INTERNALS                         SAFE EXPERIENCE FRAMING
─────────────────                           ──────────────────────
patternEngine formulas                      StudioShell room chrome
productionAssistant heuristics              DesignStudioFrame
canvas geometry / Path A useMemo            Entry, context, inspector
measurement vocabulary in types             Save / finalize **visibility**
trusted execution semantics                 Transitions, responsive host
SAC-1 finalize contract                     Trusted artifact status in chrome
```

`DesignStudio.tsx` is a **protected computational/experience asset**. SER does not rewrite it.

`DesignStudioFrame` today is a badge strip. Future stages **may** thicken the **host**:

- Arrive with client/order thread visible
- Keep geometry inside the child
- Surface SAC-1 readiness in inspector/header **without** changing `executeTrustedTailoring`
- Full-bleed canvas host; quieter chrome
- Responsive: frame scrolls; internals stay as they are until a later owner-authorized Studio programme

**Prohibits:** import of `patternEngine` into the frame; restyling canvas points; replacing Path A; AI generation; “new studio” beside the old one.

SAC-1 Path A remains live canvas. Path C remains explicit finalize.
