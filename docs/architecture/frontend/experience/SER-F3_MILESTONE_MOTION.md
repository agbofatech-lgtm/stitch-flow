# SER-F3 milestone motion

`AtelierMilestone` plays **only** when Measurement workspace has a real freeze/fingerprint message.

Does **not** wrap DesignStudio trusted finalize (protected internals). Frame-only would be F7.

Failure messages use `role=alert` with **no** success choreography.
