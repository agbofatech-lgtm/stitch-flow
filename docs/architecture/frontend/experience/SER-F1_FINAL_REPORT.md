# SER-F1 Final report

## Forensic gate

HEAD `ba808a6077186c60a9e888a681e36d3b0ace4f05` (SER-F0 docs). Source re-checked: `App.tsx` → Splash → `StudioShell` → `AtelierShell`; DesignStudio framed; `SHOP_DATA_PRECEDENCE.remoteSync === 'blocked'`; preview is a second HTML entry. Protected git-blobs unchanged.

## What F1 decided

StitchFlow is one digital atelier (rooms + thread) plus a distinct Control plane. Shell cinema without room cinema is a constitutional failure. Preview concepts KEEP/ADAPT into **product rooms**, never promote the island. Data honesty > pretty errors. Studio internals stay protected; the **frame** may grow.

## Programme

Adopt F0–F15 as in `SER-F1_PROGRAMME_ROADMAP.md`. F14 SAC UI convergence is owner-gated.

## Code

APPLICATION / BACKEND / DATABASE / CSS / PROTECTED / DEPENDENCIES: **NONE**.
