# SER-F2 accessibility foundation

Dialog: Escape, tab trap, restore focus, overlay label, `aria-modal`.  
Focus ring token. Skip link unchanged. Confidence/errors are text+badge, not color only. Reduced-motion CSS remains.
