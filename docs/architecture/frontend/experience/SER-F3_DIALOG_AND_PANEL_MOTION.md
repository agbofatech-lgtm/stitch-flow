# SER-F3 dialog and panel motion

Shared Dialog: overlay opacity + panel y (transform). Escape/focus trap unchanged. Mobile still bottom-aligned.

Sheet: +x 24px — same “tool from the side” on mobile full-width.

Inspector desktop remains a rail (no extra blur).
