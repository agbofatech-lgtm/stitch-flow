# SER-F7 visual lab

Product `/` only. Script: `apps/web/scripts/visual-lab.mjs`. Runtime: Vite `:5173`.

Design stills are captured immediately after entering the Design table, then by resizing the same session (so tablet/mobile do not depend on hamburger navigation).

| File | Pixels | Result |
|---|---|---|
| `lab/design-1280.png` | 1280×800 | Design table. Emma Thompson identity. Local workspace. Hosted — not rewritten. Finalize remains inside the studio. Open production floor. Protected studio dominates. |
| `lab/design-768.png` | 768×800 | Same identity strip. Canvas-first. Open production floor. Internal studio hero still large (protected). |
| `lab/design-390.png` | 390×844 | Thread + journey Client → Measurements → Design. Emma Thompson. Hosted studio stacks. Internal layout not redesigned. |

Motion video: **not captured**.

Known stills: last full lab pass timed out later on Measurement 390 navigation; design stills were already written. Internal studio includes a completeness banner and teal hero — those are `DesignStudio.tsx`, not the frame.
