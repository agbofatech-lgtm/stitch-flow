# SER-F3 performance

`vite build`: `main-*.js` **917.34 kB** / gzip **260.73 kB**; DataTable chunk **313.38 kB** / gzip **101.45 kB**.  
F2 main was 1043.88 / 302.34 (less split). Combined large JS similar; no new animation library.

FPS / CLS / memory: **NOT VERIFIED**. Motion uses transform/opacity.
