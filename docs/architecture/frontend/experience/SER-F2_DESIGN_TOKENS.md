# SER-F2 design tokens

Canonical CSS: `apps/web/src/experience/tokens/tokens.css`  
JS mirror: `tokens.ts` · Tailwind maps CSS variables (one system, not two palettes).

New/explicit in F2: `--sf-surface-subtle`, `--sf-overlay-dim`, `--sf-touch-min` (44px), `--sf-confidence-*`.

Dark tokens remain for Control plane only.
