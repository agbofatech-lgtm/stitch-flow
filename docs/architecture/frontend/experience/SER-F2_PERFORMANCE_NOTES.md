# SER-F2 performance notes

`vite build` (this stage): `main-*.js` **1043.88 kB** / gzip **302.34 kB**.  
Prior SAC-4 measure: 1041.19 / 301.57. Delta ~+2.7 kB / +0.8 kB gzip.

No new animation library. Experience preview remains a separate Rollup input. Runtime FPS not measured.
