# SAC-1 Mandate

**Authorized:** Studio UX + trusted save convergence only.  
**Not authorized:** SAC-2–5, 3D, Phase 20, shop API mount, PostgreSQL, PSP, FeatureGate commercial migration.

**Principle:** Interactive canvas exploration and authoritative tailoring execution must not be forcibly identical.

```
BEFORE: Path A live Studio; Path C exists elsewhere
AFTER:  Path A live Studio
        + explicit Finalize → completeness gate → freeze → executeTrustedTailoring → artifact
```
