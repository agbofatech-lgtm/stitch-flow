# SAC-3 — Authenticated Shop API

Internal authenticated `/shop` API. Distinct from `/auth`, `/platform`, `/control`.  
Legacy unauthenticated CRUD remains **off**. Persistence: **memory**. Postgres not claimed.  
SAC-4 locked.
