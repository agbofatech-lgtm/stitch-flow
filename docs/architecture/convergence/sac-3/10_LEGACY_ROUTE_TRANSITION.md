# Legacy Route Transition

`MOUNT_UNAUTHENTICATED_BUSINESS_ROUTES` remains default false. `/customers` etc. not production authority. `/shop` is the forward boundary. No destructive deletion of legacy routers.
