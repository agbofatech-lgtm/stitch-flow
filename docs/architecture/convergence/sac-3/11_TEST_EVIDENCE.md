# Test Evidence

`apps/backend/tests/shop.sac3.test.ts` — unauth, invalid JWT, no membership, ownership injection ignored, cross-tenant, workspace spoof, stage guards, artifact immutability, legacy `/customers` 404.

Environment: backend `npm install` / Jest may be **NOT VERIFIED** if ts-jest missing.
