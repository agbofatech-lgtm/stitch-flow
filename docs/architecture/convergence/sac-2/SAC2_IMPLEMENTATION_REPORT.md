# SAC-2 Implementation Report

Files: `application/shop/*` (not `data/`, which is gitignored for platform file store), `EntityRepository.putLocalCanonical`, `AppContext` post-save projection, `main.tsx` bootstrap projection.

DesignStudio not modified. No shop routes mounted. No Postgres. No localStorage deletion.
