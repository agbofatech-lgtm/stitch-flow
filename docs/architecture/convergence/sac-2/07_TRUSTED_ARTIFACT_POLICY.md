# Trusted Artifact Policy

Class D. `putLocalCanonical` refuses overwrite when `payload.frozen === true`.

`readTrustedExecutionById` is lookup only.

SAC-1 `finalizeDesignForTrustedTailoring` still does not write AppContext as artifact authority.
