# STITCHFLOW PHASE 6 CHANGELOG

Running log of Phase 6 work. Each subsystem follows: INSPECT → BASELINE → IMPLEMENT → TEST → REVIEW DIFF → COMMIT → PUSH → REMOTE VERIFY.

Baseline: `d377182` (tag `phase-5-commercial-foundation-complete`, remote-verified). Phase 6 checkpoint tag `phase-6-before-production-hardening` → `d377182`.

## Entries

- **P6-0 — Session bootstrap & audit** (`792b45b`) — branch fast-forwarded to `d377182`; Phase 5 tags remote-verified; full Phase 5 regression re-executed (web tsc 0 / backend tsc 0 / lint 16 protected findings / 129+41=170 tests / smoke 13/13 / build PASS — zero discrepancy); architecture audit → `docs/PHASE6_BASELINE.md`; risk register opened.
- **P6-1 — Health endpoints + authoritative versioning** (`855cd16`) — `/health`, `/health/live`, `/health/ready` (DB probe, 2s bounded, sanitized failures; live≠ready semantics), `/health/version` backed by `config/version.ts` (package.json source of truth; SOURCE_COMMIT/SOURCE_VERSION deploy metadata); 7 tests.
- **P6-2 — Reliability + observability** (`2c2e02f`) — DB pool hardening (limits/timeouts/statement_timeout/pool error handler); graceful shutdown (SIGTERM/SIGINT: stop accepting → close idle → drain pool → exit; hard timeout; 4 tests with mocked pool); `X-Request-Id` correlation (validated passthrough, echoed header, pino+error-body+audit shared id); in-process metrics registry (counters + latency histogram, live getters, admin-only `/admin/metrics` snapshot); pino redact + recursive `redactDeep` utility (tested on nested/Map/Error/circular); migration 012 (audit_logs.workspace_id/request_id + indexes; entity_id UUID→TEXT widening for non-UUID business ids); audit coverage (CUSTOMER_CREATED/UPDATED, ORDER_CREATED/UPDATED/STATUS_CHANGED w/ from→to, INVOICE_CREATED, MATERIAL_USED/RESTORED transactional, WORKSPACE_MEMBER_ADDED/REMOVED); AsyncLocalStorage request context. 158/158.
- **P6-3 — Android/PWA/config hardening** (`a8e2db4`) — removed dev LAN IP cleartext (`network_security_config.xml` denies cleartext globally), `usesCleartextTraffic=false`, `allowBackup=false`, `androidScheme: https` (root + mobile capacitor configs aligned to com.stitchflow.app/StitchFlow), Android versionName 1.0.0 (was 1.0), `MAX_PAYLOAD_SIZE` code default aligned to documented 1mb. Secret/bundle scan CLEAN.
- **P6-4 — Data integrity auditor + backup/restore** (`9c92752`) — `scripts/integrity-check.js` (read-only invariant checker: financial/inventory/tenant/sync/referential; exit-code semantics; `integrity:check` npm script); `scripts/db-backup.js` (REPEATABLE READ snapshot, 25 tables FK-ordered, sha256 manifest, lossless numerics via raw JSON text); `scripts/db-restore.js` (checksum gate, single transaction, count verification, sync sequence rebase); `tests/backup-restore.test.ts` — full drill on embedded PG with a second freshly-migrated database + independent direct-SQL verification (schema inventory, tenancy containment, JSONB measurement equality, financial chains + totals incl. decimal scale, inventory, subscription/billing ledger, audit correlation, cursor monotonicity, tamper refusal). 169/169.
- **P6-5 — Security regression + error taxonomy** (`7a3ee8a`) — errorHandler maps framework errors (413 PAYLOAD_TOO_LARGE, malformed JSON 400 VALIDATION_ERROR — previously masked as 500); order route input guards (dates/numerics → 400); `tests/security-regression.test.ts` 11/11 incl. **live production-mode server verification** (CORS allowlist reflection, helmet header set, real auth rate limit → 429 + standard headers, 413 behavior, SIGTERM shutdown timing) and in-process injection/XSS/UUID/malformed-input tests. 180/180.
- **P6-6 — Performance baseline** (`db15c01`) — measured (not asserted) performance: health p95 ~2 ms, customer-list(300 rows) p95 8.57 ms, dashboard p95 7.88 ms, sync delta pull p95 7.71 ms, sync mutation batch 1.45 ms/mutation; EXPLAIN ANALYZE — all high-value queries index-backed (0.016–0.195 ms); bundle metrics (1624.7 KB JS, main chunk 1221.2 KB — recorded as P2 enhancement, no speculative refactor). Report: `docs/PHASE6_PERFORMANCE_REPORT.md`; artifact `tests/perf-results.json`.
- **P6-7 — Documentation + PWA manifest brand fix** (this commit) — PHASE6_{ENVIRONMENT_MATRIX, DATABASE_HARDENING, VERSIONING, BACKUP_RESTORE_RUNBOOK, SECURITY_AUDIT, DEPLOYMENT_RUNBOOK, OPERATIONS_RUNBOOK, PERFORMANCE_REPORT, AI_EXTENSION_ARCHITECTURE, AUTOMATION_EXTENSION_ARCHITECTURE, RELEASE_CHECKLIST}.md; PWA manifest renamed TailorPro → StitchFlow (metadata only).
- **P6-8 — Final regression + certification** (final commit) — full battery re-run; protected-IP zero-diff vs `d377182`; final report.

## Session incident log (transparency)

- Mid-phase, the sandbox was re-cloned (local git metadata reset to the initial commit) while the working tree retained all content. Recovery: fetched the session branch from origin and `git reset --mixed a8e2db4` — branch pointer restored to the pushed tip, zero working-tree loss, zero history rewrite, no force-push. Uncommitted-at-the-time work (integrity + backup/restore subsystem) was completed and committed normally afterwards.
