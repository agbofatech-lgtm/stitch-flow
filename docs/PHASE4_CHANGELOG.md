# PHASE 4 CHANGELOG
Backend: refreshTokenRepository.consume() single-use rotation; authService.refresh verify-then-consume; requireWorkspaceRole middleware + owner/admin gating on settings/member mutations; invoice money validation (finite/non-negative, POST+PUT); payment_created/payment_failed audit events; env-driven CORS (prod allowlist); apiRateLimit general limiter; errorHandler requestId correlation; +14 tests (83/83 total).
Client: submitPaymentWithOfflineFallback + pending-sync banner in Invoices (3.5-R2); AccountPanel (login/register/logout/manual sync, pending-mutation-safe logout) mounted in Settings; getAuthWorkspaceId claim reader; PWA via vite-plugin-pwa (shell precache, API denylist, no runtime caching, autoUpdate) registered fail-safe in main.tsx; ESLint established (58→16 findings, remainder = protected-file documented exceptions); typed all non-protected anys (invoicePdf/jobCardPdf/Materials/Orders/tests/bridge/engine); +3 vitest tests (41/41 total).
Dependencies: npm audit fix (semver-compatible only): prod vulns 12→2 (1 low dev-server esbuild, 1 moderate uuid non-exploited path); majors declined with dispositions.
Docs: PHASE4_{BASELINE,SECURITY_MATRIX,SYNC_RELIABILITY,DATABASE_MIGRATION_GUIDE,BACKUP_RECOVERY,RISK_REGISTER,CHANGELOG,FINAL_REPORT}.
Protected systems: patternEngine/productionAssistant/DesignStudio 0 functional changes (verified by diff audit; see final report).
